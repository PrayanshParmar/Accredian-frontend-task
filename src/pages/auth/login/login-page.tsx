import LoginForm from "@/components/forms/auth/login-form";

const LoginPage = () => {
  return (
    <div className=" w-full h-full flex items-center justify-center bg-gradient-to-r from-primary to-primary-foreground">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
